extends ../layout/index

block contenido
   div.py-3
      h1(class="text-4xl my-10 font-extrabold text-center") Bienes 
         span.font-normal.text-indigo-700 Raices
      h2(class="text-center text-3xl font-extrabold")= page

   div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto")

      p(class= error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase= mensaje

      if !error
         a(href="/auth/login" class="text-center font-bold text-sm text-indigo-600 uppercase mt-10 block") Ya Puedes Iniciar Sesión